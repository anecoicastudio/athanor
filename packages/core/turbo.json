{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/supabase/migrations/*.sql",
        "$TURBO_ROOT$/supabase/functions/_shared/version-gate.ts"
      ]
    }
  }
}
