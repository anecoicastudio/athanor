{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/supabase/config.toml",
        "$TURBO_ROOT$/supabase/migrations/*.sql",
        "$TURBO_ROOT$/packages/i18n/src/catalogs/*.json",
        "$TURBO_ROOT$/packages/core/src/media/limits.ts",
        "$TURBO_ROOT$/supabase/functions/_shared/notif-templates.ts"
      ]
    }
  }
}
