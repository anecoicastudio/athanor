# Playwright e2e — secrets for AUTHENTICATED admin flows (future slices).
# Copy to .env.test (gitignored). The current admin smoke tests (e2e/admin.spec.ts)
# do NOT need this file — they only require NEXT_PUBLIC_SUPABASE_URL /
# NEXT_PUBLIC_SUPABASE_ANON_KEY (in .env.local, or CI repo secrets).
#
# Service-role key: server-side test setup ONLY (seeding an admin user / minting a
# session). NEVER ship it to the browser, never prefix it NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=eyJ...
